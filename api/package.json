{
  "name": "tbx-challenge-api",
  "version": "1.0.0",
  "description": "API REST que consume y reformatea archivos CSV desde el API externo de TBX",
  "main": "src/server.js",
  "engines": {
    "node": ">=14.0.0"
  },
  "scripts": {
    "start": "node src/server.js",
    "test": "mocha --recursive --exit --timeout 10000 \"test/**/*.test.js\"",
    "lint": "standard",
    "lint:fix": "standard --fix"
  },
  "dependencies": {
    "axios": "^0.27.2",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "chai": "^4.3.7",
    "mocha": "^10.2.0",
    "nock": "^13.3.1",
    "standard": "^17.1.0",
    "supertest": "^6.3.3"
  },
  "standard": {
    "env": [
      "mocha"
    ]
  },
  "license": "MIT"
}
